"""Internal helpers for entry/batch.py.

This package keeps batch.py's auxiliary modules in one place. The top-level
batch.py only handles CLI parsing, config loading, and step expansion; runtime
scheduling and the optional sitecustomize hook live here.
"""
