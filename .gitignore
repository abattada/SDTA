# Anchored to the repository root: an unanchored `data/` would also match
# model/PatchTST/src/data/, silently dropping that package's source files.
/data/
/outputs/
__pycache__/
*.pyc

# Build product of scripts/make_code_appendix.sh (never committed).
/code_appendix.zip
